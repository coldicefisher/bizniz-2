"""Pydantic schemas package.

Skeleton-shipped: ``app/schemas/auth.py`` (auth + user identity).
Engineers should add ``app/schemas/<feature>.py`` for their domain
schemas — never edit ``auth.py``. See SKELETON.md.
"""
