[pytest]
addopts = -m "not functional"
markers =
    functional: tests that call real external APIs (OpenAI). Run with: pytest -m functional
    smoke: end-to-end smoke tests that build images and bring stacks up. Run with: pytest -m "functional and smoke"
    timeout: per-test timeout in seconds (provided by pytest-timeout)